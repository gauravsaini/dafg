/**
 * Health check endpoint — /health
 */
function registerHealthRoutes(router, Route) {
  // Health is already registered in server.js — this is a no-op placeholder
  // for consistency with the route index.
}

module.exports = { registerHealthRoutes };